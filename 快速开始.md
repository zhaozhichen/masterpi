# 快速开始指南

## 第一步：环境配置

### 1. 运行自动配置脚本

```bash
cd /home/pi
./setup.sh
```

### 2. 手动配置（如果脚本失败）

```bash
# 创建虚拟环境
python3 -m venv venv
source venv/bin/activate

# 安装依赖
pip install --upgrade pip
pip install -r requirements.txt
```

### 3. 检查系统配置

```bash
# 启用I2C（如果未启用）
sudo raspi-config
# 选择: Interface Options -> I2C -> Enable

# 启用摄像头（如果未启用）
sudo raspi-config
# 选择: Interface Options -> Camera -> Enable

# 检查I2C设备
sudo i2cdetect -y 1
# 应该能看到0x7A设备
```

## 第二步：启动系统

### 方式A：启动GUI控制界面（最简单）

```bash
cd /home/pi
source venv/bin/activate
cd MasterPi_PC_Software
python3 Arm.py
```

**GUI界面功能：**
- ✅ 实时控制6个舵机（滑块）
- ✅ 编辑和运行动作组
- ✅ 摄像头颜色识别调试
- ✅ 舵机偏差校准

### 方式B：启动机器人主程序

```bash
cd /home/pi
source venv/bin/activate
cd MasterPi
python3 MasterPi.py
```

**主程序功能：**
- ✅ 启动RPC服务器（端口9030）
- ✅ 启动摄像头流服务器（端口8080）
- ✅ 运行功能模块（避障、颜色跟踪等）

### 方式C：同时运行（推荐）

**终端1：启动主程序**
```bash
cd /home/pi
source venv/bin/activate
cd MasterPi
python3 MasterPi.py
```

**终端2：启动GUI**
```bash
cd /home/pi
source venv/bin/activate
cd MasterPi_PC_Software
python3 Arm.py
```

## 第三步：测试硬件

### 测试舵机

在Python交互式环境中：

```python
import sys
sys.path.append('/home/pi/MasterPi_PC_Software')
import SetPWMServo as PWM
import time

# 测试舵机1
PWM.setPWMServoPulse(1, 1500, 1000)  # 中位
time.sleep(1)
PWM.setPWMServoPulse(1, 2000, 1000)  # 右转
time.sleep(1)
PWM.setPWMServoPulse(1, 1000, 1000)  # 左转
time.sleep(1)
PWM.setPWMServoPulse(1, 1500, 1000)  # 回到中位
```

### 测试摄像头

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import Camera
import cv2
import time

cam = Camera.Camera()
cam.camera_open()
time.sleep(1)

if cam.frame is not None:
    print("摄像头工作正常！")
    cv2.imshow('test', cam.frame)
    cv2.waitKey(0)
    cv2.destroyAllWindows()
else:
    print("摄像头无法读取图像")

cam.camera_close()
```

### 测试马达

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board
import time

# 测试马达1
Board.setMotor(1, 50)   # 正转
time.sleep(2)
Board.setMotor(1, 0)     # 停止
```

## 常用操作

### 控制舵机

```python
# 导入模块
import sys
sys.path.append('/home/pi/MasterPi_PC_Software')
import SetPWMServo as PWM

# 单个舵机
PWM.setPWMServoPulse(1, 1500, 1000)  # 舵机ID, 脉冲值, 时间(ms)

# 多个舵机同时
PWM.setPWMServosPulse([1000, 5, 1, 1500, 3, 1500, 4, 1500, 5, 1500, 6, 1500])
```

### 控制马达

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board

Board.setMotor(1, 50)   # 马达ID, 速度(-100到100)
```

### 读取摄像头

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import Camera

cam = Camera.Camera()
cam.camera_open()
frame = cam.frame  # 获取当前帧
```

## 文件说明

- `启动指南.md` - 详细的系统说明文档
- `硬件控制参考.md` - 所有硬件控制API参考
- `requirements.txt` - Python依赖包列表
- `setup.sh` - 自动环境配置脚本

## 常见问题

### Q: 导入模块失败
**A:** 确保已激活虚拟环境：`source venv/bin/activate`

### Q: I2C通信失败
**A:** 
1. 检查I2C是否启用：`sudo raspi-config`
2. 检查设备：`sudo i2cdetect -y 1`

### Q: 摄像头无法打开
**A:**
1. 检查摄像头连接
2. 检查权限：`sudo usermod -a -G video $USER`
3. 尝试重启

### Q: 舵机不响应
**A:**
1. 检查I2C连接
2. 检查电源
3. 确认舵机ID正确（1, 3, 4, 5, 6）

## 下一步

1. ✅ 熟悉GUI界面操作
2. ✅ 创建和编辑动作组
3. ✅ 开发自定义功能模块
4. ✅ 使用RPC接口进行远程控制

## 获取帮助

- 查看 `启动指南.md` 获取详细说明
- 查看 `硬件控制参考.md` 获取API文档
- 查看示例代码：`MasterPi/board_demo/` 和 `MasterPi/Functions/`


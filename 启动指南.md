# 树莓派机器人系统启动指南

## 系统概述

这是一个基于树莓派的机械臂机器人控制系统，包含以下主要功能：
- **舵机控制**：6个PWM舵机（ID: 1, 3, 4, 5, 6）
- **马达控制**：4个直流马达（用于麦轮底盘）
- **摄像头**：OpenCV图像采集和处理
- **超声波传感器**：距离检测
- **RGB LED**：状态指示
- **GUI界面**：PyQt5图形界面（Arm.py）

## 项目结构

```
/home/pi/
├── MasterPi_PC_Software/    # PC端控制软件（GUI）
│   ├── Arm.py               # 主入口程序（GUI界面）
│   ├── SetPWMServo.py       # PWM舵机控制
│   └── ServoCmd.py          # 舵机命令封装
├── MasterPi/                # 机器人主程序
│   ├── MasterPi.py          # 主程序入口
│   ├── Camera.py            # 摄像头控制
│   ├── Board.py             # 硬件板控制（舵机、马达、电池等）
│   ├── HiwonderSDK/         # SDK库
│   │   ├── Board.py         # 硬件板接口
│   │   ├── mecanum.py       # 麦轮底盘控制
│   │   └── Sonar.py         # 超声波传感器
│   ├── Functions/           # 功能模块
│   └── ActionGroups/        # 动作组文件（.d6a格式）
└── requirements.txt          # Python依赖包列表
```

## 环境配置

### 1. 创建Python虚拟环境

```bash
cd /home/pi
python3 -m venv venv
source venv/bin/activate
```

### 2. 安装依赖包

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

### 3. 系统依赖

确保以下系统服务已启用：
- **I2C接口**：`sudo raspi-config` → Interface Options → I2C → Enable
- **摄像头**：`sudo raspi-config` → Interface Options → Camera → Enable

### 4. 检查硬件连接

- I2C设备地址：0x7A（扩展板）
- 摄像头：USB或CSI接口
- 舵机：通过I2C扩展板连接
- 马达：通过I2C扩展板连接

## 如何控制硬件

### 舵机控制

**方法1：使用SetPWMServo模块（PC端）**
```python
import sys
sys.path.append('/home/pi/MasterPi_PC_Software')
import SetPWMServo as PWM

# 控制单个舵机
# setPWMServoPulse(servo_id, pulse, use_time)
# servo_id: 1-6
# pulse: 500-2500 (1500为中位)
# use_time: 0-30000 (毫秒)
PWM.setPWMServoPulse(1, 1500, 1000)  # 舵机1转到中位，用时1秒

# 同时控制多个舵机
# setPWMServosPulse([time, number, id1, pos1, id2, pos2, ...])
PWM.setPWMServosPulse([1000, 5, 1, 1500, 3, 1500, 4, 1500, 5, 1500, 6, 1500])
```

**方法2：使用Board模块（机器人端）**
```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board

# 控制PWM舵机
Board.setPWMServoPulse(servo_id, pulse, use_time)
Board.setPWMServosPulse([time, number, id1, pos1, id2, pos2, ...])
```

### 马达控制

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board

# 控制单个马达
# setMotor(index, speed)
# index: 1-4 (马达编号)
# speed: -100 到 100 (负数为反转)
Board.setMotor(1, 50)   # 马达1正转，速度50%
Board.setMotor(2, -30)  # 马达2反转，速度30%
Board.setMotor(1, 0)    # 停止马达1

# 使用麦轮底盘控制（推荐）
import HiwonderSDK.mecanum as mecanum
chassis = mecanum.MecanumChassis()
chassis.set_velocity(velocity, direction, angular_rate)
# velocity: 速度 (mm/s)
# direction: 方向 (0-360度，0度为前方)
# angular_rate: 角速度
chassis.set_velocity(70, 0, 0)  # 向前移动
chassis.set_velocity(0, 0, 0)   # 停止
```

### 摄像头输入

**方法1：直接使用Camera类**
```python
import sys
sys.path.append('/home/pi/MasterPi/')
import Camera

cam = Camera.Camera()
cam.camera_open()

while True:
    if cam.frame is not None:
        frame = cam.frame.copy()
        # 处理图像
        cv2.imshow('frame', frame)
        if cv2.waitKey(1) & 0xFF == ord('q'):
            break

cam.camera_close()
```

**方法2：通过HTTP流（mjpg-streamer）**
```python
import cv2

# 从HTTP流读取
cap = cv2.VideoCapture('http://127.0.0.1:8080?action=stream')
while True:
    ret, frame = cap.read()
    if ret:
        # 处理图像
        cv2.imshow('frame', frame)
        if cv2.waitKey(1) & 0xFF == ord('q'):
            break
cap.release()
```

### 超声波传感器

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Sonar as Sonar

sonar = Sonar.Sonar()
distance = sonar.getDistance()  # 返回距离（单位：mm）

# 控制RGB LED
sonar.setRGBMode(0)  # 0=关闭，1=呼吸模式
sonar.setPixelColor(0, Board.PixelColor(255, 0, 0))  # 设置LED0为红色
sonar.setPixelColor(1, Board.PixelColor(0, 255, 0))  # 设置LED1为绿色
```

### 其他硬件

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board

# 读取电池电压
voltage = Board.getBattery()  # 返回毫伏值

# 控制蜂鸣器
Board.setBuzzer(1)  # 打开
Board.setBuzzer(0)  # 关闭
```

## 启动方式

### 方式1：启动GUI控制界面（推荐）

```bash
cd /home/pi
source venv/bin/activate
cd MasterPi_PC_Software
python3 Arm.py
```

**GUI功能：**
- 舵机实时控制（滑块）
- 动作组编辑和运行
- 摄像头颜色识别调试
- 舵机偏差校准

### 方式2：启动机器人主程序

```bash
cd /home/pi
source venv/bin/activate
cd MasterPi
python3 MasterPi.py
```

**主程序功能：**
- 启动RPC服务器（端口9030）
- 启动MJPG流服务器（端口8080）
- 运行各种功能模块（避障、颜色跟踪等）

### 方式3：同时运行（两个终端）

**终端1：启动主程序**
```bash
cd /home/pi
source venv/bin/activate
cd MasterPi
python3 MasterPi.py
```

**终端2：启动GUI**
```bash
cd /home/pi
source venv/bin/activate
cd MasterPi_PC_Software
python3 Arm.py
```

## 配置文件

### 舵机偏差文件
`/home/pi/MasterPi/Deviation.yaml`
```yaml
'1': 0
'3': 0
'4': 0
'5': 0
'6': 0
```

### 颜色识别配置
`/home/pi/MasterPi/lab_config.yaml`
```yaml
red:
  max: [255, 255, 255]
  min: [0, 150, 130]
green:
  max: [255, 110, 255]
  min: [47, 0, 135]
```

## 常见问题

### 1. I2C通信失败
- 检查I2C是否启用：`sudo i2cdetect -y 1`
- 检查设备地址：应该能看到0x7A

### 2. 摄像头无法打开
- 检查摄像头连接
- 检查权限：`sudo usermod -a -G video $USER`
- 尝试重启：`sudo reboot`

### 3. 舵机不响应
- 检查I2C连接
- 检查电源供应
- 检查舵机ID是否正确（1, 3, 4, 5, 6）

### 4. 导入模块错误
- 确保使用虚拟环境：`source venv/bin/activate`
- 检查Python路径：`echo $PYTHONPATH`
- 确保在正确的目录运行

## 快速测试

### 测试舵机
```python
import sys
sys.path.append('/home/pi/MasterPi_PC_Software')
import SetPWMServo as PWM
import time

# 测试舵机1
PWM.setPWMServoPulse(1, 1500, 1000)  # 中位
time.sleep(1)
PWM.setPWMServoPulse(1, 2000, 1000)  # 右转
time.sleep(1)
PWM.setPWMServoPulse(1, 1000, 1000)  # 左转
time.sleep(1)
PWM.setPWMServoPulse(1, 1500, 1000)  # 回到中位
```

### 测试摄像头
```python
import sys
sys.path.append('/home/pi/MasterPi/')
import Camera
import cv2

cam = Camera.Camera()
cam.camera_open()
time.sleep(1)  # 等待摄像头初始化

if cam.frame is not None:
    print("摄像头工作正常！")
    cv2.imshow('test', cam.frame)
    cv2.waitKey(0)
else:
    print("摄像头无法读取图像")

cam.camera_close()
```

### 测试马达
```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board
import time

# 测试马达1
Board.setMotor(1, 50)   # 正转
time.sleep(2)
Board.setMotor(1, 0)     # 停止
```

## 下一步

1. **熟悉GUI界面**：运行`Arm.py`，尝试控制舵机
2. **创建动作组**：在GUI中编辑和保存动作序列
3. **开发功能模块**：在`MasterPi/Functions/`目录下创建新功能
4. **使用RPC接口**：通过JSON-RPC远程控制机器人

## 技术支持

- 检查日志文件
- 查看硬件连接
- 参考示例代码：`MasterPi/board_demo/`和`MasterPi/Functions/`


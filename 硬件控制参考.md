# 硬件控制快速参考

## 舵机控制

### 基本控制函数

```python
# 导入模块
import sys
sys.path.append('/home/pi/MasterPi_PC_Software')
import SetPWMServo as PWM

# 或使用机器人端模块
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board
```

### 单个舵机控制

```python
# setPWMServoPulse(servo_id, pulse, use_time)
# 参数说明：
#   servo_id: 1, 3, 4, 5, 6 (舵机ID)
#   pulse: 500-2500 (1500为中位，500最左，2500最右)
#   use_time: 0-30000 (毫秒，转动时间)

PWM.setPWMServoPulse(1, 1500, 1000)  # 舵机1转到中位，用时1秒
PWM.setPWMServoPulse(1, 2000, 500)   # 舵机1快速右转
PWM.setPWMServoPulse(1, 1000, 2000)  # 舵机1慢速左转
```

### 多舵机同步控制

```python
# setPWMServosPulse([time, number, id1, pos1, id2, pos2, ...])
# 参数说明：
#   time: 执行时间（毫秒）
#   number: 舵机数量
#   id1, pos1: 舵机ID和位置
#   可以同时控制多个舵机

# 同时控制5个舵机到中位
PWM.setPWMServosPulse([1000, 5, 1, 1500, 3, 1500, 4, 1500, 5, 1500, 6, 1500])

# 舵机1和3同时转动
PWM.setPWMServosPulse([800, 2, 1, 2000, 3, 1000])
```

### 读取舵机位置

```python
# 获取舵机当前脉冲值
pulse = PWM.getPWMServoPulse(1)  # 返回500-2500之间的值

# 获取舵机当前角度（近似值）
angle = PWM.getPWMServoAngle(1)  # 返回0-180之间的角度值
```

## 马达控制

### 单个马达控制

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board

# setMotor(index, speed)
# 参数说明：
#   index: 1-4 (马达编号)
#   speed: -100 到 100 (负数为反转，0为停止)

Board.setMotor(1, 50)   # 马达1正转，速度50%
Board.setMotor(1, -30)  # 马达1反转，速度30%
Board.setMotor(1, 0)    # 停止马达1
```

### 麦轮底盘控制（推荐）

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.mecanum as mecanum

# 创建底盘对象
chassis = mecanum.MecanumChassis()

# 设置速度（极坐标方式）
# set_velocity(velocity, direction, angular_rate)
#   velocity: 速度 (mm/s)
#   direction: 方向角度 (0-360度，0度为前方，180度为后方)
#   angular_rate: 角速度 (旋转速度)

chassis.set_velocity(70, 0, 0)      # 向前移动
chassis.set_velocity(70, 90, 0)    # 向右移动
chassis.set_velocity(70, 180, 0)   # 向后移动
chassis.set_velocity(70, 270, 0)   # 向左移动
chassis.set_velocity(0, 0, 50)     # 原地旋转
chassis.set_velocity(0, 0, 0)      # 停止

# 平移控制（笛卡尔坐标）
# translation(velocity_x, velocity_y)
chassis.translation(70, 0)    # 向右平移
chassis.translation(0, 70)   # 向前平移
chassis.translation(-70, 0)  # 向左平移
chassis.translation(0, -70)  # 向后平移

# 重置所有马达
chassis.reset_motors()
```

## 摄像头输入

### 方法1：使用Camera类（推荐）

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import Camera
import cv2
import time

# 创建摄像头对象
cam = Camera.Camera(resolution=(640, 480))

# 打开摄像头
cam.camera_open()
time.sleep(1)  # 等待初始化

# 读取图像
while True:
    if cam.frame is not None:
        frame = cam.frame.copy()
        # 处理图像
        cv2.imshow('Camera', frame)
        if cv2.waitKey(1) & 0xFF == ord('q'):
            break
    time.sleep(0.03)

# 关闭摄像头
cam.camera_close()
cv2.destroyAllWindows()
```

### 方法2：使用OpenCV直接读取

```python
import cv2

# 打开USB摄像头
cap = cv2.VideoCapture(-1)  # -1表示自动选择摄像头
cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)
cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 480)

while True:
    ret, frame = cap.read()
    if ret:
        # 处理图像
        cv2.imshow('Camera', frame)
        if cv2.waitKey(1) & 0xFF == ord('q'):
            break
    else:
        print("无法读取图像")
        break

cap.release()
cv2.destroyAllWindows()
```

### 方法3：从HTTP流读取（需要先启动MasterPi.py）

```python
import cv2

# 从mjpg-streamer读取
cap = cv2.VideoCapture('http://127.0.0.1:8080?action=stream')

while True:
    ret, frame = cap.read()
    if ret:
        cv2.imshow('Stream', frame)
        if cv2.waitKey(1) & 0xFF == ord('q'):
            break
    else:
        print("无法连接流服务器")
        break

cap.release()
cv2.destroyAllWindows()
```

## 超声波传感器

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Sonar as Sonar
import HiwonderSDK.Board as Board

# 创建超声波对象
sonar = Sonar.Sonar()

# 读取距离
distance = sonar.getDistance()  # 返回距离（单位：mm，最大5000mm）
print(f"距离: {distance}mm")

# 控制RGB LED
# 设置LED模式（0=关闭，1=呼吸模式）
sonar.setRGBMode(0)

# 设置LED颜色
# setPixelColor(index, color)
#   index: 0 或 1 (LED编号)
#   color: Board.PixelColor(r, g, b) 或 (r, g, b) 元组
sonar.setPixelColor(0, Board.PixelColor(255, 0, 0))    # LED0设为红色
sonar.setPixelColor(1, Board.PixelColor(0, 255, 0))    # LED1设为绿色

# 设置呼吸效果
# setBreathCycle(index, rgb, cycle)
#   index: 0 或 1
#   rgb: 0=红, 1=绿, 2=蓝
#   cycle: 呼吸周期（毫秒）
sonar.setBreathCycle(0, 0, 2000)  # LED0红色呼吸，周期2秒

# 启动交响模式（自动呼吸效果）
sonar.startSymphony()
```

## 其他硬件

### 电池电压

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board

# 读取电池电压（返回毫伏值）
voltage_mv = Board.getBattery()
voltage_v = voltage_mv / 1000.0
print(f"电池电压: {voltage_v}V")
```

### 蜂鸣器

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board
import time

# 控制蜂鸣器
Board.setBuzzer(1)  # 打开
time.sleep(0.5)     # 响0.5秒
Board.setBuzzer(0)   # 关闭
```

### RGB LED（扩展板）

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.Board as Board

# 设置RGB LED颜色
# RGB是全局对象，已在Board模块中初始化
Board.RGB.setPixelColor(0, Board.PixelColor(255, 0, 0))  # LED0红色
Board.RGB.setPixelColor(1, Board.PixelColor(0, 255, 0))  # LED1绿色
Board.RGB.show()  # 更新显示
```

## 动作组控制

### 运行动作组

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.ActionGroupControl as AGC

# 运行动作组（异步）
# runAction(action_name)
#   action_name: 动作组文件名（不含.d6a扩展名）
AGC.runAction('stand')  # 运行stand.d6a动作组

# 停止动作组
AGC.stop_action_group()
```

### 在PC端控制

```python
import sys
sys.path.append('/home/pi/MasterPi_PC_Software')
from ServoCmd import *

# 运行动作组
runActionGroup('stand')

# 停止动作组
stopActionGroup()
```

## 完整示例

### 示例1：舵机扫描

```python
import sys
sys.path.append('/home/pi/MasterPi_PC_Software')
import SetPWMServo as PWM
import time

# 舵机1左右扫描
for i in range(3):
    PWM.setPWMServoPulse(1, 2000, 500)  # 右转
    time.sleep(0.6)
    PWM.setPWMServoPulse(1, 1000, 500)  # 左转
    time.sleep(0.6)
PWM.setPWMServoPulse(1, 1500, 500)      # 回到中位
```

### 示例2：底盘移动

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import HiwonderSDK.mecanum as mecanum
import time

chassis = mecanum.MecanumChassis()

# 前进2秒
chassis.set_velocity(70, 0, 0)
time.sleep(2)

# 右转1秒
chassis.set_velocity(70, 90, 0)
time.sleep(1)

# 停止
chassis.set_velocity(0, 0, 0)
```

### 示例3：摄像头颜色检测

```python
import sys
sys.path.append('/home/pi/MasterPi/')
import Camera
import cv2
import numpy as np

cam = Camera.Camera()
cam.camera_open()
time.sleep(1)

# 颜色阈值（LAB色彩空间）
red_min = np.array([0, 150, 130])
red_max = np.array([255, 255, 255])

while True:
    if cam.frame is not None:
        frame = cam.frame.copy()
        frame_lab = cv2.cvtColor(frame, cv2.COLOR_BGR2LAB)
        mask = cv2.inRange(frame_lab, red_min, red_max)
        
        # 查找轮廓
        contours, _ = cv2.findContours(mask, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
        if contours:
            # 找到最大轮廓
            max_contour = max(contours, key=cv2.contourArea)
            if cv2.contourArea(max_contour) > 100:
                x, y, w, h = cv2.boundingRect(max_contour)
                cv2.rectangle(frame, (x, y), (x+w, y+h), (0, 255, 0), 2)
        
        cv2.imshow('Camera', frame)
        cv2.imshow('Mask', mask)
        if cv2.waitKey(1) & 0xFF == ord('q'):
            break

cam.camera_close()
cv2.destroyAllWindows()
```

## 注意事项

1. **舵机ID**：系统使用舵机ID为 1, 3, 4, 5, 6（没有ID 2）
2. **脉冲范围**：舵机脉冲值必须在500-2500之间
3. **I2C地址**：扩展板I2C地址为0x7A
4. **摄像头**：需要先启动MasterPi.py才能使用HTTP流
5. **虚拟环境**：建议始终在venv中运行代码
6. **权限**：某些操作可能需要sudo权限


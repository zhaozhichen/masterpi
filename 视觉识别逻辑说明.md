# 机器人视觉识别逻辑说明

## 一、颜色识别流程

### 1. 图像预处理
```python
# 步骤1: 图像缩放
frame_resize = cv2.resize(img_copy, size, interpolation=cv2.INTER_NEAREST)
# size = (640, 480) - 将图像缩放到固定尺寸

# 步骤2: 高斯模糊（降噪）
frame_gb = cv2.GaussianBlur(frame_resize, (3, 3), 3)

# 步骤3: 色彩空间转换
frame_lab = cv2.cvtColor(frame_gb, cv2.COLOR_BGR2LAB)
# 从BGR转换到LAB色彩空间（对光照变化更鲁棒）
```

### 2. 颜色阈值过滤
```python
# 使用LAB色彩空间的阈值范围
frame_mask = cv2.inRange(frame_lab,
    (lab_data[i]['min'][0], lab_data[i]['min'][1], lab_data[i]['min'][2]),
    (lab_data[i]['max'][0], lab_data[i]['max'][1], lab_data[i]['max'][2]))
```

**当前配置（lab_config.yaml）：**
- **红色**: min=[0, 145, 130], max=[255, 255, 255]
- **绿色**: min=[0, 0, 0], max=[200, 120, 150]
- **蓝色**: min=[0, 0, 0], max=[255, 255, 115]

### 3. 形态学处理
```python
# 开运算：去除小噪点
opened = cv2.morphologyEx(frame_mask, cv2.MORPH_OPEN, np.ones((3, 3), np.uint8))

# 闭运算：填充小孔洞
closed = cv2.morphologyEx(opened, cv2.MORPH_CLOSE, np.ones((3, 3), np.uint8))
```

### 4. 轮廓检测和筛选
```python
# 查找轮廓
contours = cv2.findContours(closed, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_NONE)[-2]

# 找出面积最大的轮廓（过滤面积 < 300 的轮廓）
areaMaxContour, area_max = getAreaMaxContour(contours)

# 面积阈值：max_area > 2500 才认为检测到方块
if max_area > 2500:
    # 检测到有效目标
```

### 5. 颜色判断逻辑
```python
# 多次判断取平均值（提高稳定性）
color_list.append(color)  # 每次检测结果加入列表
if len(color_list) == 3:  # 累积3次判断
    color = int(round(np.mean(np.array(color_list))))  # 取平均值
    color_list = []
    start_pick_up = True  # 开始抓取
```

## 二、位置计算流程

### 1. 最小外接矩形
```python
# 获取方块的最小外接矩形
rect = cv2.minAreaRect(areaMaxContour_max)
# rect 包含: ((center_x, center_y), (width, height), angle)

# 获取矩形的四个顶点
box = np.int32(cv2.boxPoints(rect))
```

### 2. 中心点计算（Transform.py - getCenter函数）

**关键问题：当前代码中 ColorSorting 似乎没有使用 getCenter 函数！**

理论上应该这样计算：
```python
# 1. 获取ROI区域
roi = getROI(box)  # (x_min, x_max, y_min, y_max)

# 2. 计算精确中心点（考虑旋转）
center_x, center_y = getCenter(rect, roi, size, square_length)
# square_length = 3cm（方块边长）

# 3. 转换为世界坐标
world_X, world_Y = convertCoordinate(center_x, center_y, size)
```

### 3. 坐标转换（Transform.py - convertCoordinate函数）
```python
# 图像坐标 → 世界坐标（单位：cm）
x = leMap(x, 0, size[0], 0, 640)  # 归一化到640宽度
x = x - 320  # 以图像中心为原点
x_ = round(x * map_param_, 2)  # 乘以映射参数

y = leMap(y, 0, size[1], 0, 480)  # 归一化到480高度
y = 240 - y  # 翻转Y轴
y_ = round(y * map_param_ + image_center_distance, 2)  # 加上中心距离偏移
# image_center_distance = 20cm（机械臂原点到摄像头中心的距离）
```

## 三、可能的不准确原因

### 1. 颜色识别不准确

**问题1：LAB阈值范围设置不当**
- 当前红色阈值：min=[0, 145, 130] 范围太大，可能误识别
- 绿色阈值：min=[0, 0, 0] 完全没有下限，会识别到所有颜色
- 蓝色阈值：min=[0, 0, 0] 同样没有下限

**解决方案：**
```bash
# 使用 lab_adjust.py 重新校准颜色阈值
cd /home/pi/MasterPi/Functions
sudo -E /home/pi/venv/bin/python lab_adjust.py
```

**问题2：光照条件变化**
- LAB色彩空间虽然对光照有一定鲁棒性，但极端光照仍会影响识别
- 建议在固定光照条件下校准和使用

**问题3：形态学处理参数**
- 当前使用 3x3 核，可能无法有效去除噪点或填充孔洞
- 可以尝试调整核大小：`np.ones((5, 5), np.uint8)`

### 2. 位置计算不准确

**问题1：ColorSorting 中未使用 getCenter 函数**
- 当前代码似乎直接使用 `minAreaRect` 的中心点，没有考虑方块旋转
- 应该使用 `getCenter()` 函数计算精确中心

**问题2：映射参数 map_param 可能不准确**
- 这个参数来自相机标定，如果标定不准确会影响位置计算
- 需要重新进行相机标定

**问题3：image_center_distance 可能不准确**
- 当前设置为 20cm，如果实际距离不同会影响Y坐标

**问题4：方块旋转角度未考虑**
- `getCenter()` 函数考虑了旋转角度，但 ColorSorting 中可能没有使用

### 3. 其他问题

**问题1：面积阈值 2500 可能不合适**
- 如果方块距离较远，面积会小于2500，无法检测
- 如果背景有相似颜色的区域，可能误检测

**问题2：多次判断机制**
- 当前累积3次判断取平均值，如果前两次判断错误，第三次正确，平均值可能还是错的
- 建议改为：3次中有2次相同才确认

## 四、改进建议

### 1. 重新校准颜色阈值

```bash
# 运行颜色校准工具
cd /home/pi/MasterPi/Functions
sudo -E /home/pi/venv/bin/python lab_adjust.py

# 或者使用GUI工具
cd /home/pi/MasterPi_PC_Software
python3 Arm.py  # 在"Camera Tool"标签页中调整
```

### 2. 检查并修复位置计算

检查 `ColorSorting.py` 是否使用了 `getCenter()` 和 `convertCoordinate()` 函数。如果没有，需要添加：

```python
# 在检测到方块后
if max_area > 2500:
    rect = cv2.minAreaRect(areaMaxContour_max)
    box = np.int32(cv2.boxPoints(rect))
    
    # 计算ROI
    roi = getROI(box)
    
    # 计算精确中心点
    center_x, center_y = getCenter(rect, roi, size, square_length)
    
    # 转换为世界坐标
    world_X, world_Y = convertCoordinate(center_x, center_y, size)
    
    # 使用 world_X, world_Y 进行抓取
```

### 3. 改进颜色判断逻辑

```python
# 改为：3次中有2次相同才确认
if len(color_list) == 3:
    # 统计每种颜色出现的次数
    from collections import Counter
    color_counts = Counter(color_list)
    most_common = color_counts.most_common(1)[0]
    
    if most_common[1] >= 2:  # 至少2次相同
        color = most_common[0]
        start_pick_up = True
    else:
        color_list = []  # 重新判断
```

### 4. 调整检测参数

```python
# 根据实际情况调整
max_area_threshold = 2500  # 可以降低到 1500-2000
morphology_kernel_size = 3  # 可以增加到 5
```

### 5. 添加调试输出

```python
# 在检测到方块时输出调试信息
print(f"检测到颜色: {color_area_max}, 面积: {max_area}, 中心: ({center_x}, {center_y}), 世界坐标: ({world_X}, {world_Y})")
```

## 五、调试步骤

1. **检查颜色阈值配置**
   ```bash
   cat /home/pi/MasterPi/lab_config.yaml
   ```

2. **运行颜色校准工具重新校准**
   ```bash
   cd /home/pi/MasterPi/Functions
   sudo -E /home/pi/venv/bin/python lab_adjust.py
   ```

3. **检查相机标定参数**
   ```bash
   # 检查标定文件是否存在
   ls -la /home/pi/MasterPi/CameraCalibration/calibration_param.npz
   ls -la /home/pi/MasterPi/CameraCalibration/map_param.npz
   ```

4. **添加调试输出查看实际检测值**
   - 在代码中添加 print 语句输出检测到的颜色、面积、坐标等

5. **检查代码是否使用了正确的坐标转换函数**
   - 确认 ColorSorting.py 中是否调用了 getCenter() 和 convertCoordinate()

## 六、关键参数说明

| 参数 | 位置 | 说明 | 默认值 |
|------|------|------|--------|
| `square_length` | CalibrationConfig.py | 方块边长（cm） | 3 |
| `image_center_distance` | Transform.py | 机械臂原点到摄像头中心距离（cm） | 20 |
| `map_param` | map_param.npz | 像素到实际距离的映射参数 | 来自标定 |
| `max_area_threshold` | ColorDetect.py | 最小检测面积（像素²） | 2500 |
| `morphology_kernel` | ColorDetect.py | 形态学处理核大小 | 3x3 |
| `color_list_size` | ColorDetect.py | 颜色判断次数 | 3 |

## 总结

**主要问题可能是：**
1. ✅ LAB颜色阈值设置不当（特别是绿色和蓝色没有下限）
2. ✅ ColorSorting 可能没有使用精确的中心点计算函数
3. ✅ 相机标定参数可能不准确
4. ✅ 光照条件影响识别

**建议优先处理：**
1. 使用 lab_adjust.py 重新校准颜色阈值
2. 检查并修复位置计算代码
3. 添加调试输出查看实际检测值

